import { RemoteComponentDirective } from './remote-component.directive';

describe('RemoteComponentDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoteComponentDirective();
    expect(directive).toBeTruthy();
  });
});
